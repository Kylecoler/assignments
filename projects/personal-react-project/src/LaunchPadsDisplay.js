import React from 'react'

const LaunchPadsDisplay = (props)=>{
    return(
        <div style={{textAlign:"center"}}>
            <h1>{props.name}</h1>
        </div>
    )
}

export default LaunchPadsDisplay