import React from 'react'

const RocketsDisplay = (props)=>{
    return(
        <div style={{textAlign:"center"}}>
            <h1>Rocket Id:{props.name}</h1>
        </div>
    )
}

export default RocketsDisplay