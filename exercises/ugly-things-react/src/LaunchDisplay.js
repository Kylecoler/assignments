import React from 'react'

const LaunchDisplay =(props)=>{
    return(
        <div style={{textAlign:"center"}}>
            <h1>{props.name}</h1>
        </div>
    )
}

export default LaunchDisplay